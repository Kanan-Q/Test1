@model DepartmentUpdateVM;

<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">Department create</h6>
    </div>
    <div class="card-body">
        <form method="post">
            <div class="form-group">
                <label asp-for="DepartmentName">Title</label>
                <input class="form-control" asp-for="DepartmentName">
                <span class="text-danger" asp-validation-for="DepartmentName"></span>
            </div>

            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>
</div>
